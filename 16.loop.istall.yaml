- name: Install packages
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansibe.builtin.package:
      name: "{{ item }}"
      state: latest
    loops:
    - nginx
    - mysql
    - zip